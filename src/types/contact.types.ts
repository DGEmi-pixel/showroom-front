export interface Contact {
    phoneNumber: string,
    supportMail: string,
    country: string,
    socialMedia: {
        facebook: string,
        whatsapp: string,
        instagram: string
    }
}