export interface Contact {
    phoneNumber: string,
    supportMail: string,
    country: string,
    socialMedia: {
        facebook: string,
        twitter: string,
        instagram: string
    }
}